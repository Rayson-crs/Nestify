{
  "name": "nestify",
  "version": "1.9.1",
  "private": true,
  "description": "Rule-driven local file governance workbench",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "sync:version": "node scripts/sync-app-version.mjs",
    "start": "npm run sync:version && npm run start -w @nestify/desktop",
    "dev": "npm run sync:version && npm run start -w @nestify/desktop",
    "build": "npm run sync:version && npm run build -w @nestify/desktop",
    "dist": "npm run sync:version && npm run dist -w @nestify/desktop",
    "benchmark:search": "node --experimental-strip-types tools/search-benchmark.mjs",
    "postinstall": "npm run dist",
    "test": "npm test -w @nestify/core && npm test -w @nestify/rules",
    "typecheck": "npm run typecheck -w @nestify/shared && npm run typecheck -w @nestify/core && npm run typecheck -w @nestify/rules && npm run typecheck -w @nestify/desktop"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "^1.1.23"
  }
}
